version: "2"

options:
  compose-file-path: 'docker-compose.base.yml'
  compose-dev-file-path: 'docker-compose.dev.yml'
  verbose: true
  max_attempt: 30

syncs:
  ${DOCKER_BASENAME}_app-sync:
    src: './html'
    sync_strategy: 'unison'
    sync_userid: '33'